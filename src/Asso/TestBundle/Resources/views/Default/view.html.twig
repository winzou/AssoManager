{% extends "::layout.html.twig" %}

{% block body %}

	<h1>Asso\TestBundle\view > Here is...</h1>
	
	{% if app.session.hasFlash('msg') %}
    <div class="flash-notice">
        {{ app.session.flash('msg') }}
    </div>
    {% endif %}

	Hello {{ name }} !
	
{% endblock %}
